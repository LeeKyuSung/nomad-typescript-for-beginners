// import { init, exit } from "./myPackage";

// init({ url: "http://localhost:3000", debug: true });
// exit(1);

import {
  head,
  // hasIn,
  // isBoolean,
  // toString,
  // split,
  // hasPath,
  // filter,
  // every,
  // map,
} from "lodash";
